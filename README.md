# BuzzFlow-Backend


## Instructions

Prereq:
- Have docker-compose

instructions:
1. get .env file from someone
2. cd to directory and run `docker-compose up`


notes:
- might need to open a shell within the container to execute `python manage.py migrate`, can choose to either use the vscode extension or cli
